import BrandCommandCenterClient from './BrandCommandCenterClient';

export const metadata = {
  title: 'Brand Command Center — OpenFang',
  description: 'Enter brand context once. Run specialist AI agents to research, write, plan, and execute.',
};

export default function BrandCommandCenterPage() {
  return <BrandCommandCenterClient />;
}
